name: Ping Streamlit WebApp

on:
  schedule:
    - cron: '*/5 * * * *' # This cron expression means every 7 days at midnight UTC
  workflow_dispatch: # Allows manual triggering of the workflow

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
    - name: Send HTTP request to Streamlit app
      run: |
        curl -X GET "https://frontendpy-h24q76f4ncj9qnnd2u6zkp.streamlit.app/"
